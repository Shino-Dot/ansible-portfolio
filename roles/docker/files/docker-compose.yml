version: '3'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db

  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
  grafana:
    image: grafana/grafana
    ports:
      - "3001:3000"
